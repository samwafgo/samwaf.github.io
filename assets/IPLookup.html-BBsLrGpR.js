import{_ as e}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as o,a,o as i}from"./app-SCCtw_Dg.js";const s={};function n(l,t){return i(),o("div",null,[...t[0]||(t[0]=[a('<h1 id="ip-lookup" tabindex="-1"><a class="header-anchor" href="#ip-lookup"><span>IP Lookup</span></a></h1><h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview"><span>Overview</span></a></h2><p>A single IP can be governed by several places at once: the IP Blacklist, IP Allowlist, IP Groups, Threat Intelligence IP feeds, IP Failure Bans, CC bans, Firewall IP Blocking — and it may also fall inside a CDN origin range. Working out &quot;why is this IP blocked?&quot; (or &quot;why isn&#39;t it?&quot;) by opening each page one by one is slow and easy to get wrong.</p><p><strong>IP Lookup</strong> collapses that into a single query: enter an IP, and it checks every source above at once and gives you a verdict — <strong>Will be allowed</strong> or <strong>Will be blocked</strong> — along with exactly which rule, which site and which feed matched. If it turns out to be a false positive you can add it to the allowlist right there; if it is genuinely malicious you can block it right there too.</p><p>Matching is <strong>CIDR-aware</strong>: looking up <code>1.2.3.4</code> matches a <code>1.2.3.0/24</code> entry in your lists — it is not a plain text comparison.</p><h2 id="where-to-open-it" tabindex="-1"><a class="header-anchor" href="#where-to-open-it"><span>Where to Open It</span></a></h2><p>The <strong>IP Lookup</strong> button is available at the top right of these pages:</p><table><thead><tr><th>Page</th><th>Location</th></tr></thead><tbody><tr><td>Protection Logs → Attack Log</td><td>Right side of the top info bar</td></tr><tr><td>Protection Logs → Visit Log</td><td>Right side of the top info bar</td></tr><tr><td>Website Protection → Threat Intelligence IP Subscription</td><td>Right side of the top info bar</td></tr><tr><td>Website Protection → CDN Origin IPs</td><td>Right side of the top info bar</td></tr><tr><td>System Settings → IP Failure Ban</td><td>Next to the Refresh button</td></tr></tbody></table><p>In addition, the IPs in the following places are <strong>clickable</strong> — one click opens the lookup and runs it automatically, no copy-and-paste needed:</p><ul><li>The <strong>IP</strong> column in the Attack Log list</li><li>The <strong>Source IP</strong> column in the Visit Log list</li><li>The <strong>IP</strong> column in the IP Failure Ban list</li><li>Every IP / CIDR in the &quot;View IPs&quot; dialog of Threat Intelligence IP Subscription</li><li>Every CIDR in the &quot;View IPs&quot; dialog of CDN Origin IPs</li></ul><h2 id="operation-steps" tabindex="-1"><a class="header-anchor" href="#operation-steps"><span>Operation Steps</span></a></h2><h3 id="look-up-an-ip" tabindex="-1"><a class="header-anchor" href="#look-up-an-ip"><span>Look Up an IP</span></a></h3><ol><li>Open <strong>IP Lookup</strong> from any of the entry points above.</li><li>Enter the IP and click <strong>Search</strong> (or just press Enter).</li><li>The query runs as 4 concurrent batches, and the progress of each is shown below the input: <strong>Lists</strong>, <strong>Bans</strong>, <strong>Threat-intel IPs</strong>, <strong>CDN origin ranges</strong>. Batches that finish first render first — you do not have to wait for all of them.</li><li>The top of the results area shows the IP, its location and the <strong>verdict</strong>; below it, every match is listed by source.</li></ol><div class="hint-container tip"><p class="hint-container-title">You can look up a range too</p><p>The input also accepts CIDRs (such as <code>1.2.3.0/24</code>) and ranges (such as <code>1.2.3.5-1.2.3.9</code>). In that case a representative IP inside the range is used, and the result states which IP was actually queried.</p></div><h3 id="allowlist-a-false-positive" tabindex="-1"><a class="header-anchor" href="#allowlist-a-false-positive"><span>Allowlist a False Positive</span></a></h3><ol><li>When the verdict is <strong>Will be blocked</strong>, an <strong>Add to allowlist</strong> button appears below the match list.</li><li>Open it and choose the site it <strong>applies to</strong> (defaults to &quot;Global Site&quot;, i.e. all sites).</li><li>The <strong>reason</strong> is pre-filled with the date and the sources that originally matched. You can edit it, but it cannot be empty.</li><li>Click <strong>Confirm</strong>. The lookup re-runs automatically so you can see the verdict change for yourself.</li></ol><div class="hint-container warning"><p class="hint-container-title">The allowlist only applies inside the WAF</p><p>If the IP is blocked at the <strong>system firewall layer</strong> (Firewall IP Blocking, or a threat-intel feed whose landing layer is &quot;System firewall&quot; or &quot;Both&quot;), an orange warning appears in the allowlist form.</p><p>The system firewall drops packets at the operating-system level, so the request never reaches the WAF and the IP allowlist <strong>cannot take effect</strong>. After allowlisting, the verdict will read &quot;Allowed by the WAF, still blocked by the system firewall&quot;.</p><p>To allow it completely, unban it on the <strong>Firewall IP Blocking</strong> page, or change the threat-intel feed&#39;s <strong>landing layer</strong> to &quot;WAF only&quot;.</p></div><h3 id="blocklist-a-malicious-ip" tabindex="-1"><a class="header-anchor" href="#blocklist-a-malicious-ip"><span>Blocklist a Malicious IP</span></a></h3><ol><li>When the IP is in neither the blocklist nor the allowlist, an <strong>Add to blocklist</strong> button appears.</li><li>Choose the site it <strong>applies to</strong> and where to <strong>block</strong> it (WAF only / System firewall / Both — defaults to &quot;WAF only&quot;).</li><li>Fill in the <strong>reason</strong> (also pre-filled).</li><li>Click <strong>Confirm</strong>. The lookup re-runs automatically.</li></ol><h2 id="verdicts" tabindex="-1"><a class="header-anchor" href="#verdicts"><span>Verdicts</span></a></h2><table><thead><tr><th>Verdict</th><th>Meaning</th></tr></thead><tbody><tr><td>Not on any list</td><td>The IP is currently not on any allow/block list, ban record or threat-intel feed</td></tr><tr><td>Will be allowed</td><td>Matched the allowlist, with no system-firewall-layer block</td></tr><tr><td>Will be blocked</td><td>Matched the blocklist / threat intelligence / one of the ban records</td></tr><tr><td>Allowed by the WAF, still blocked by the system firewall</td><td>Allowlisted, but the system firewall is still dropping packets — handle it separately</td></tr><tr><td>Matched, but does not allow or block on its own</td><td>For example only a CDN origin range matched, or the matched IP Group is not referenced by any allow/block list</td></tr></tbody></table><h2 id="field-reference" tabindex="-1"><a class="header-anchor" href="#field-reference"><span>Field Reference</span></a></h2><p>Each match in the result list contains:</p><table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td>Source</td><td>Where the match came from: IP allowlist, IP blocklist, IP group, Threat-intel IP, IP failure ban, CC ban, Firewall IP ban, CDN origin IP</td></tr><tr><td>Scope</td><td>Which site / IP group / feed channel / CDN provider the entry belongs to. Entries that apply everywhere show &quot;Global Site&quot;</td></tr><tr><td>Matched rule</td><td>The exact rule that matched (single IP, CIDR, wildcard or range). Cache-based bans have no rule text, so this is empty</td></tr><tr><td>Effect</td><td>Allow / Block / Info only</td></tr><tr><td>Details</td><td>Remarks, remaining ban time, the threat-intel landing layer, and so on</td></tr></tbody></table><h2 id="faq" tabindex="-1"><a class="header-anchor" href="#faq"><span>FAQ</span></a></h2><p><strong>Which sources are covered?</strong></p><p>IP allowlist, IP blocklist, IP groups, threat-intel IPs, IP failure bans, CC bans, firewall IP bans and CDN origin ranges — eight in total.</p><p><strong>Why is the first lookup slightly slower, and later ones fast?</strong></p><p>Threat-intel feeds and CDN origin ranges can reach hundreds of thousands of entries. The first lookup builds an index once (typically a few hundred milliseconds); after that the index is reused until the corresponding feed syncs again.</p><p><strong>What does &quot;These sources failed this time, so the result may be incomplete&quot; mean?</strong></p><p>One of the sources could not be queried (for example a snapshot is being synced). In that case &quot;not found&quot; must not be read as &quot;not on any list&quot; — try again shortly.</p><p><strong>A match says &quot;site no longer exists, this entry has no effect&quot;?</strong></p><p>The site this entry was bound to has been deleted, so the entry does nothing. You can clean it up on the corresponding allow/block list page.</p><p><strong>I allowlisted the IP but still cannot reach the site.</strong></p><p>It is most likely blocked at the system firewall layer. See <a href="#allowlist-a-false-positive">The allowlist only applies inside the WAF</a> above.</p>',35)])])}const d=e(s,[["render",n]]),c=JSON.parse('{"path":"/en/guide/IPLookup.html","title":"IP Lookup","lang":"en-US","frontmatter":{},"git":{"createdTime":1786417472000,"updatedTime":1786417472000,"contributors":[{"name":"samwaf","username":"samwaf","email":"samwafgo@gmail.com","commits":1,"url":"https://github.com/samwaf"}]},"readingTime":{"minutes":3.75,"words":1124},"filePathRelative":"en/guide/IPLookup.md"}');export{d as comp,c as data};
