import{_ as r}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as l,a as i,b as t,d as s,e as n,w as o,r as c,o as h}from"./app-SCCtw_Dg.js";const d={},p={class:"hint-container warning"};function g(u,e){const a=c("RouteLink");return h(),l("div",null,[e[15]||(e[15]=i('<h1 id="parameter-settings" tabindex="-1"><a class="header-anchor" href="#parameter-settings"><span>Parameter Settings</span></a></h1><p>Parameter Settings centrally manage SamWaf&#39;s <strong>own management console</strong> security and access configuration. They map to the related items in <code>conf/config.yml</code> and can be edited and saved from the UI.</p><blockquote><p>Menu path: System Settings → Parameter Settings</p></blockquote><h2 id="_1-management-ip-whitelist" tabindex="-1"><a class="header-anchor" href="#_1-management-ip-whitelist"><span>1 Management IP Whitelist</span></a></h2><p>Restrict the source IPs allowed to access the SamWaf console. CIDR is supported. Click <strong>Save</strong> to apply.</p>',5)),t("div",p,[e[9]||(e[9]=t("p",{class:"hint-container-title"},"Warning",-1)),t("p",null,[e[1]||(e[1]=s("A wrong whitelist can lock you out of the console. To recover, edit ",-1)),e[2]||(e[2]=t("code",null,"security.ip_whitelist",-1)),e[3]||(e[3]=s(" in ",-1)),e[4]||(e[4]=t("code",null,"conf/config.yml",-1)),e[5]||(e[5]=s(" (e.g. set it to ",-1)),e[6]||(e[6]=t("code",null,"0.0.0.0/0,::/0",-1)),e[7]||(e[7]=s(") and restart — see the ",-1)),n(a,{to:"/faq/"},{default:o(()=>[...e[0]||(e[0]=[s("FAQ",-1)])]),_:1}),e[8]||(e[8]=s(".",-1))])]),e[16]||(e[16]=i(`<h2 id="_2-domain-whitelist" tabindex="-1"><a class="header-anchor" href="#_2-domain-whitelist"><span>2 Domain Whitelist</span></a></h2><p>Restrict which domains can access the console. Click <strong>Save</strong> to apply.</p><h2 id="_3-management-ssl" tabindex="-1"><a class="header-anchor" href="#_3-management-ssl"><span>3 Management SSL</span></a></h2><p>Enable HTTPS access for the console.</p><ul><li><strong>Enable SSL</strong>: the console supports HTTPS once enabled.</li><li><strong>Force HTTPS</strong>: the console only allows HTTPS access.</li><li><strong>Certificate status</strong>: shows whether a certificate is uploaded, plus its domain and expiry time.</li><li><strong>Certificate / Private key</strong>: paste the PEM certificate and key; or click <strong>Select from certificate folder</strong> to copy in one step, or <strong>Upload certificate</strong> to save.</li><li><strong>Bind certificate-folder certificate</strong>: once bound, it auto-syncs — when the certificate in the folder is updated, the console certificate follows. You can unbind at any time.</li><li><strong>Restart Manager</strong>: after changing SSL-related settings, click <strong>Restart Manager</strong> to apply.</li></ul><h3 id="_3-1-getting-a-certificate-for-the-console" tabindex="-1"><a class="header-anchor" href="#_3-1-getting-a-certificate-for-the-console"><span>3.1 Getting a certificate for the console</span></a></h3><p>The console is different from a protected site: it is usually <strong>not exposed to the internet</strong>, so file validation (HTTP-01) is not an option — that method requires Let&#39;s Encrypt to reach your port 80 from the public internet.</p><p><strong>If you have a domain, use DNS validation (DNS-01).</strong> Validation only touches DNS records, so it needs <strong>no port 80 and no public exposure of the console</strong>.</p><p>Supported DNS providers: Alibaba Cloud, Huawei Cloud, Tencent Cloud, Cloudflare, Baidu Cloud.</p><p>Steps:</p><ol><li>Go to <strong>SSL Certificates → Automatic certificate request → New</strong>, choose <strong>DNS validation</strong> as the request method, and fill in the domain for the console plus the applicant email;</li><li>Once issued, come back to this page and use <strong>Bind certificate-folder certificate</strong> to select it;</li><li>Click <strong>Restart Manager</strong> to apply.</li></ol><p>After binding you get <strong>automatic renewal and automatic refresh</strong>: the certificate in the folder renews 30 days before expiry and the console certificate follows. This is why binding is easier than pasting the certificate content — the latter has to be pasted again after every renewal.</p><div class="hint-container tip"><p class="hint-container-title">The domain does not need to resolve publicly</p><p>DNS-01 validates that you control the DNS records for the domain, regardless of which IP it points to. Pointing the domain at an internal address such as <code>192.168.x.x</code> works fine and still gets a certificate issued.</p></div><div class="hint-container warning"><p class="hint-container-title">No domain at all</p><p>If you only have an IP address and not even an internal domain name, DNS-01 cannot be used. See the next section on local certificates.</p></div><h3 id="_3-2-local-certificate-when-you-have-no-domain" tabindex="-1"><a class="header-anchor" href="#_3-2-local-certificate-when-you-have-no-domain"><span>3.2 Local certificate (when you have no domain)</span></a></h3><p>If you only have an IP address — or you have a domain but do not want to go through ACME — SamWaf can generate a long-lived CA on this machine and use it to sign the console certificate.</p><div class="hint-container warning"><p class="hint-container-title">One thing to be clear about</p><p>A local certificate gives <strong>exactly the same encryption strength as a public one</strong> — TLS strength has nothing to do with who signed the certificate. The only difference is that browsers do not know this CA by default, so you import the root CA once on <strong>the computer you browse from</strong>. After that it is a normal green padlock.</p></div><h4 id="steps" tabindex="-1"><a class="header-anchor" href="#steps"><span>Steps</span></a></h4><ol><li><p>Turn on <strong>Enable SSL</strong> in the <strong>Management SSL</strong> card;</p></li><li><p>In <strong>Local certificate</strong>, enter <strong>every address you actually use to reach the console</strong>, comma separated. For example:</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>192.168.1.10,waf.internal,localhost,127.0.0.1</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><p>Click <strong>Generate local certificate</strong>;</p></li><li><p>Click <strong>Download root CA</strong> to get <code>samwaf-local-ca.crt</code> and import it on the computers you browse from (next section);</p></li><li><p>Click <strong>Restart Manager</strong> to apply.</p></li></ol><div class="hint-container caution"><p class="hint-container-title">Do not leave any access address out</p><p>Modern browsers only look at the SAN list in the certificate; they <strong>ignore</strong> the CommonName. Every address you use to reach the console must appear in the field above, otherwise that address will fail with a certificate error. Common misses: entering only the domain while habitually browsing by IP, or entering only the internal IP after having reached it once over a public IP. Listing extra addresses does no harm; leaving one out does.</p></div><h4 id="importing-the-root-ca" tabindex="-1"><a class="header-anchor" href="#importing-the-root-ca"><span>Importing the root CA</span></a></h4><p><strong>Windows</strong> (the store location is the step people get wrong - follow all three):</p><ol><li>Double-click <code>samwaf-local-ca.crt</code> → <strong>Install Certificate</strong> → store location <strong>Local Machine</strong> (Current User works if it is only for you).</li><li><strong>The key step</strong>: choose &quot;<strong>Place all certificates in the following store</strong>&quot;. Do <strong>not</strong> leave &quot;Automatically select the certificate store based on the type of certificate&quot; selected - that puts it under <em>Personal</em>, where it has no effect.</li><li>Click <strong>Browse</strong> → select &quot;<strong>Trusted Root Certification Authorities</strong>&quot; → OK → Next → Finish, and confirm the security warning with <strong>Yes</strong>.</li></ol><p><strong>Restart the browser</strong> afterwards: Chrome and Edge only read the system trust store at startup, so without a restart they keep showing &quot;Not secure&quot;.</p><p><strong>macOS</strong>: double-click to import into the <strong>login</strong> or <strong>System</strong> keychain in Keychain Access → find <code>SamWaf Local CA</code>, double-click → expand <strong>Trust</strong> → set &quot;When using this certificate&quot; to <strong>Always Trust</strong>. Restart the browser.</p><p><strong>Linux (Debian/Ubuntu)</strong>:</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> cp</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwaf-local-ca.crt</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> /usr/local/share/ca-certificates/samwaf-local-ca.crt</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> update-ca-certificates</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Linux (RHEL/CentOS)</strong>:</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> cp</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwaf-local-ca.crt</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> /etc/pki/ca-trust/source/anchors/</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> update-ca-trust</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Firefox</strong> keeps its own trust store and does not read the system one, so import separately: Settings → Privacy &amp; Security → Certificates → View Certificates → <strong>Authorities</strong> tab → Import → tick &quot;Trust this CA to identify websites&quot;.</p><h4 id="renewal" tabindex="-1"><a class="header-anchor" href="#renewal"><span>Renewal</span></a></h4><ul><li>The certificate is <strong>reissued automatically</strong> when fewer than <strong>30 days</strong> remain; the card always shows the days left;</li><li>You can also click <strong>Reissue / renew</strong> at any time (use the same button after changing the access addresses);</li><li><strong>The CA does not change, so computers that already imported the root CA never need to import it again.</strong> This is precisely why a two-level &quot;CA + server certificate&quot; design is used instead of a single self-signed certificate.</li></ul><div class="hint-container tip"><p class="hint-container-title">Why not a 10-year validity</p><p>The CA itself lasts 10 years, but the server certificate <strong>cannot exceed 825 days</strong> — a hard limit Apple platforms impose on TLS certificates from any CA; beyond it Safari and iOS reject the certificate outright. Hence the long-lived CA plus a short-lived, auto-renewing certificate.</p></div><h4 id="working-out-which-root-ca-to-delete" tabindex="-1"><a class="header-anchor" href="#working-out-which-root-ca-to-delete"><span>Working out which root CA to delete</span></a></h4><p>The <strong>Local certificate</strong> section shows the SHA-256 <strong>fingerprint</strong> of the current root CA.</p><p>This matters once you have rebuilt the root CA: the trust store then holds <strong>several entries all named <code>SamWaf Local CA</code></strong>, and only the fingerprint tells them apart. The one whose fingerprint <strong>matches the page</strong> is in use (deleting it brings the &quot;Not secure&quot; warning back); any entry with a different fingerprint is left over from before the rebuild and is safe to delete.</p><h4 id="removing-the-root-ca" tabindex="-1"><a class="header-anchor" href="#removing-the-root-ca"><span>Removing the root CA</span></a></h4><p><strong>Windows</strong>: press <code>Win+R</code> and run <code>certlm.msc</code> (use <code>certmgr.msc</code> if you installed it for the current user) → expand <strong>Trusted Root Certification Authorities → Certificates</strong> → find <code>SamWaf Local CA</code> → double-click to check the fingerprint → right-click and delete.</p><p><strong>macOS</strong>: open <strong>Keychain Access</strong> → search for <code>SamWaf</code> in the System or login keychain → double-click to check the fingerprint → right-click and <strong>Delete</strong>.</p><p><strong>Linux</strong>:</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"># Debian / Ubuntu</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> rm</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> /usr/local/share/ca-certificates/samwaf-local-ca.crt</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> update-ca-certificates</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --fresh</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;"># RHEL / CentOS</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> rm</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> /etc/pki/ca-trust/source/anchors/samwaf-local-ca.crt</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">sudo</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> update-ca-trust</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>Firefox</strong>: Settings → Privacy &amp; Security → Certificates → View Certificates → <strong>Authorities</strong> → select <code>SamWaf Local CA</code> → Delete or Distrust.</p><h4 id="rebuilding-the-root-ca-the-equivalent-of-revocation" tabindex="-1"><a class="header-anchor" href="#rebuilding-the-root-ca-the-equivalent-of-revocation"><span>Rebuilding the root CA (the equivalent of revocation)</span></a></h4><p>A self-managed CA has no revocation service (CRL/OCSP), so there is <strong>no true revocation</strong>. If the root CA private key may have leaked, or you simply want a different one, the answer is to switch to a new CA:</p><p><strong>Local certificate → expand Destructive actions → Rebuild root CA</strong>.</p><div class="hint-container caution"><p class="hint-container-title">This is destructive</p><p>The new root CA has no relationship to the old one. <strong>Every computer that imported the old root CA will immediately mark the console as insecure</strong> and must delete the old entry and import the new one using the steps above. A restart of the manager is required as well.</p></div><h4 id="removing-the-local-certificate" tabindex="-1"><a class="header-anchor" href="#removing-the-local-certificate"><span>Removing the local certificate</span></a></h4><p>If you no longer want to use a local certificate (for example you switched to a public one), use <strong>Local certificate → expand Destructive actions → Remove local certificate</strong>. This deletes the local root CA and the console certificate it signed.</p><p>Two notes:</p><ul><li>If the console is <strong>currently using</strong> the local certificate, the request is refused - turn off SSL or switch to another certificate source first, otherwise the console would find no certificate to load after a restart;</li><li>Only certificates issued by the local CA are touched. Manually uploaded certificates and certificates bound from the certificate folder <strong>are never deleted by mistake</strong>.</li></ul><p>After removing it, remember to delete the root CA on the computers that imported it as well (same steps as above).</p><h2 id="_4-security-entry-path" tabindex="-1"><a class="header-anchor" href="#_4-security-entry-path"><span>4 Security Entry Path</span></a></h2><p>Add a &quot;security code&quot; prefix path to the console to hide the admin entry. Once enabled, every access must include the prefix: <code>http(s)://host:port/{code}/...</code>.</p><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>The security path applies to all requests of the entire HTTP server, so it affects <strong>more than the admin UI</strong> — the WebSocket (<code>/api/v1/ws</code>), the Open Platform API, and any <code>/api/v1/...</code> call must all use the prefixed URL.</p></div><ul><li>Turn on <strong>Enable</strong> and enter a custom code; if the custom code is left empty when saving, the backend generates an 18-character random code.</li><li>After saving, the page shows the full access URL, which you can <strong>Copy URL</strong>, <strong>Open</strong>, or <strong>Regenerate</strong>.</li></ul><h2 id="_5-notification-title-prefix" tabindex="-1"><a class="header-anchor" href="#_5-notification-title-prefix"><span>5 Notification Title Prefix</span></a></h2><p>Set a prefix for notification message titles, to distinguish the source among alerts from multiple SamWaf nodes. Click <strong>Save</strong> after editing.</p><h2 id="_6-management-trusted-proxies" tabindex="-1"><a class="header-anchor" href="#_6-management-trusted-proxies"><span>6 Management Trusted Proxies</span></a></h2><p>When the SamWaf console runs behind a reverse proxy (e.g. Nginx) or a CDN, this tells SamWaf which direct sources are trusted proxies so it can identify the real client IP correctly. <strong>The admin IP whitelist / login lockout / token IP binding are all judged by the identified IP.</strong></p><p>There is a master switch <strong>Admin behind proxy/CDN</strong> at the top:</p><ul><li><strong>Off</strong>: always judge by the direct network-layer IP (keep off when the console is NOT behind a proxy/CDN — safest). The fields below are hidden.</li><li><strong>On</strong>: identify the real client IP from proxy headers and reveal the config below. The switch reflects whether the proxy header has a value — non-empty means on.</li></ul><p>Once on, you configure &quot;which header to read&quot; and &quot;which sources to trust&quot;:</p><h3 id="_6-1-management-proxy-header" tabindex="-1"><a class="header-anchor" href="#_6-1-management-proxy-header"><span>6.1 Management Proxy Header</span></a></h3><p>Comma-separated request headers by priority, e.g. <code>X-Forwarded-For,X-Real-IP,CF-Connecting-IP</code>. <strong>Empty = don&#39;t parse proxy headers, use the network-layer IP.</strong> Quick-fill tags below the input append common headers (Cloudflare/Fastly/AWS CloudFront/EdgeOne/Aliyun/Akamai real-IP headers); you can still edit manually.</p><blockquote><p>This is the <code>gwaf_manage_proxy_header</code> parameter from Settings; changes here take effect immediately. It differs from the visitor IP header (<code>gwaf_proxy_header</code>), which applies to business sites.</p></blockquote><h3 id="_6-2-trusted-sources-1-reference-cdn-∪-2-manual-cidrs-combined" tabindex="-1"><a class="header-anchor" href="#_6-2-trusted-sources-1-reference-cdn-∪-2-manual-cidrs-combined"><span>6.2 Trusted sources (① Reference CDN ∪ ② Manual CIDRs, combined)</span></a></h3><p>Only when the management request&#39;s direct source is a trusted proxy does SamWaf trust the real client IP from the header above; otherwise it falls back to the network-layer IP, preventing spoofing. <strong>Trusted proxy = ① referenced CDN origin ranges ∪ ② manual CIDRs</strong>, combined (a hit in either is trusted):</p>`,67)),t("ul",null,[t("li",null,[e[11]||(e[11]=t("strong",null,"① Reference CDN",-1)),e[12]||(e[12]=s(": if the console is also behind a CDN, selecting a vendor auto-trusts its origin ranges (reads the latest central value, auto-updated, no manual entry). Ranges are managed on the ",-1)),n(a,{to:"/en/guide/CDNIP.html"},{default:o(()=>[...e[10]||(e[10]=[s("CDN Origin IPs",-1)])]),_:1}),e[13]||(e[13]=s(" page; once selected, the vendor's central-store downloaded count is shown here.",-1))]),e[14]||(e[14]=t("li",null,[t("strong",null,"② Manual Trusted Proxies"),s(": for cases the CDN central store can't cover (e.g. a self-hosted Nginx / internal load balancer behind the CDN). CIDR or IP, comma-separated, e.g. "),t("code",null,"10.0.0.0/8,192.168.0.0/16"),s(". "),t("strong",null,"Empty = no manual entry"),s(" (the referenced CDN vendor above still applies).")],-1))]),e[17]||(e[17]=i('<p>When using only one, leave the other empty.</p><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>The manual CIDRs are stored in <code>conf/config.yml</code>. If this locks you out via the IP whitelist, edit the file and restart to recover.</p></div><h2 id="_7-cors-allow-origins" tabindex="-1"><a class="header-anchor" href="#_7-cors-allow-origins"><span>7 CORS Allow Origins</span></a></h2><p>Controls which origins may access the management API cross-origin (CORS), preventing arbitrary sites from making credentialed cross-origin requests.</p><ul><li><strong>Effect</strong>: loopback / local origins (<code>127.0.0.1</code>, <code>localhost</code>) are always allowed, so local access and local development need no setup; only add <strong>remotely-deployed</strong> frontend origins here. <strong>Empty = loopback only</strong>.</li><li><strong>Value</strong>: origins, comma-separated, e.g. <code>https://waf.example.com,http://192.168.1.10:8080</code>.</li><li>Same-origin access (frontend on the same host and port as the console) is not cross-origin and needs no setup.</li></ul><div class="hint-container warning"><p class="hint-container-title">Warning</p><p>Stored in <code>conf/config.yml</code>. If a wrong CORS config locks out a remote frontend, edit the file and restart to recover.</p></div>',6))])}const y=r(d,[["render",g]]),k=JSON.parse('{"path":"/en/guide/VpConfig.html","title":"Parameter Settings","lang":"en-US","frontmatter":{},"git":{"createdTime":1782802574000,"updatedTime":1787708255000,"contributors":[{"name":"samwaf","username":"samwaf","email":"samwafgo@gmail.com","commits":4,"url":"https://github.com/samwaf"}]},"readingTime":{"minutes":7.27,"words":2181},"filePathRelative":"en/guide/VpConfig.md"}');export{y as comp,k as data};
