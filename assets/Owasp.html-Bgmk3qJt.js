import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as e,a as r,o}from"./app-jnUMVP8R.js";const s={};function n(d,a){return o(),e("div",null,[...a[0]||(a[0]=[r('<h1 id="owasp-规则集管理" tabindex="-1"><a class="header-anchor" href="#owasp-规则集管理"><span>OWASP 规则集管理</span></a></h1><h2 id="功能简介" tabindex="-1"><a class="header-anchor" href="#功能简介"><span>功能简介</span></a></h2><p>OWASP 核心规则集（CRS）在线管理页面，用于管理 SamWaf 内置的 OWASP CRS（基于 Coraza 引擎）规则。它把规则查看、调参、命中统计、在线升级、沙盒测试、变更记录和使用说明整合在一个页面，通过顶部的标签页（Tab）切换。</p><p>支持的能力包括：规则列表查看、启用 / 禁用 / 改写规则、阈值与 PL（Paranoia Level，偏执级别）调整、在线升级（保留用户改动）、沙盒测试、命中统计与使用说明。</p><p>页面共有 7 个标签页：<strong>规则管理、调参、命中统计、在线升级、沙盒测试、变更记录、使用说明</strong>。</p><h2 id="标签页说明" tabindex="-1"><a class="header-anchor" href="#标签页说明"><span>标签页说明</span></a></h2><h3 id="一、规则管理" tabindex="-1"><a class="header-anchor" href="#一、规则管理"><span>一、规则管理</span></a></h3><p>查看与管理每一条 CRS 规则。</p><ul><li><strong>筛选</strong>：可按所属文件、严重级别、PL、状态（默认 / 已禁用 / 已改写）筛选，也可用关键字 / 规则 ID / 描述搜索；支持「我的改动」只看自己改过的规则，以及「热重载」让改动立即生效。</li><li><strong>操作</strong>：对单条规则可执行 <strong>查看 / 启用 / 禁用 / 改写 / 还原</strong>。 <ul><li><strong>禁用</strong>：会在 <code>overrides/10-disabled-rules.conf</code> 中添加 <code>SecRuleRemoveById</code>，不修改原始规则文件，热重载后生效。</li><li><strong>改写</strong>：使用 Coraza / ModSecurity 语法编辑规则内容，保存后写入 <code>overrides/20-custom-rules.conf</code>，<strong>上游升级不会覆盖</strong>。</li><li><strong>AI 分析</strong>：可将规则内容与元数据发送给 AI 助手，解释该规则的防护意图、触发条件与可能的误报场景（发送前请确认内容）。</li></ul></li><li><strong>字段含义</strong>：详情中提供规则 ID、执行阶段（Phase 1~5）、严重级别、Paranoia Level、标签、描述、指令类型等说明。</li></ul><h3 id="二、调参" tabindex="-1"><a class="header-anchor" href="#二、调参"><span>二、调参</span></a></h3><p>调整规则引擎的整体行为与阈值。SamWaf 默认配置已比官方更宽松以降低误报，修改后会自动热重载生效。</p><p>主要参数：</p><ul><li><strong>引擎模式</strong>：开启拦截（On）/ 仅观察（DetectionOnly）/ 关闭（Off）。</li><li><strong>拦截 Paranoia 级别</strong>：范围 1-4，级别越高越严格，默认 1。</li><li><strong>检测 Paranoia 级别</strong>：必须 ≥ 拦截 PL，大于拦截 PL 的规则仅记录不拦截，默认 2。</li><li><strong>入站 / 出站 Anomaly 阈值</strong>：累计风险分达到该值才触发拦截。入站官方默认 5、SamWaf 默认更宽松；出站默认 4。</li><li><strong>提前拦截（early_blocking）</strong>：开启后会额外在 Phase 1（仅读完请求头）再做一次评估。无论开关状态，Phase 2（读完请求体后）的标准拦截始终生效。</li><li><strong>请求体大小上限 / 内存处理上限 / 检测字节上限</strong>：控制 Coraza 缓冲与检测的请求体大小，单位字节，<code>0</code> 多为使用默认值。</li><li><strong>CRS 事务变量（tx.*）</strong>：以 <code>SecAction setvar</code> 形式写入 <code>overrides/00-tuning.conf</code>，比直接改写规则更安全，升级 CRS 时不会丢失。</li></ul><blockquote><p>若只想对特定 URL 或 IP 跳过 OWASP 检测，无需修改全局参数，可前往「URL 白名单」或「IP 白名单」配置。</p></blockquote><h3 id="三、命中统计" tabindex="-1"><a class="header-anchor" href="#三、命中统计"><span>三、命中统计</span></a></h3><p>统计运行期间（重启清零）各规则的命中次数，帮助识别高频误报规则，为禁用或调参提供依据。</p><ul><li>可按总次数 / 拦截次数 / 观察次数排序，并查看总拦截、总观察、命中最多规则等汇总。</li><li>列表展示严重程度、规则说明、总命中、拦截、观察、最后命中等。</li><li>支持「清空统计」（操作不可恢复），以及从某条规则直接「查看规则」跳转到规则管理。</li></ul><h3 id="四、在线升级" tabindex="-1"><a class="header-anchor" href="#四、在线升级"><span>四、在线升级</span></a></h3><p>通过 SamWaf 升级服务器拉取最新规则包。升级会<strong>保留您已改写的规则 ID</strong>，仅更新上游版本。</p><ul><li>显示当前版本、最新版本、是否需要更新、最后检查时间与更新说明。</li><li>点击 <strong>检查更新</strong> 比对版本，点击 <strong>立即升级</strong> 在后台执行升级，完成后通过消息推送通知。</li></ul><h3 id="五、沙盒测试" tabindex="-1"><a class="header-anchor" href="#五、沙盒测试"><span>五、沙盒测试</span></a></h3><p>构造一次 HTTP 请求，直接喂给当前 Coraza 实例，查看命中的规则与 anomaly 分，用于验证调参或改写效果。</p><ul><li>可填写方法、URL、头部（每行一个 <code>Key: Value</code>）、Body，也可「加载示例」。</li><li>运行后展示结果：是否拦截、拦截累计分、检测累计分、拦截阈值，以及命中规则明细（级别、分值、是否计入拦截、阶段、规则消息、规则文件）。</li><li>结果中可直接对命中规则执行 <strong>禁用 / 改写</strong>，并给出诊断提示（如命中但未达阈值、因 PL 过滤未计入拦截分等）。</li></ul><h3 id="六、变更记录" tabindex="-1"><a class="header-anchor" href="#六、变更记录"><span>六、变更记录</span></a></h3><p>记录通过 SamWaf 界面或 API 发起的规则操作（手动编辑文件不会写入此记录）。</p><ul><li>列表包含时间、规则 ID、操作类型（禁用 / 启用 / 改写 / 还原 / 调参）、规则来源文件、备注。</li><li>可刷新，并从记录跳转到对应规则。</li></ul><h3 id="七、使用说明" tabindex="-1"><a class="header-anchor" href="#七、使用说明"><span>七、使用说明</span></a></h3><p>页面内置的使用说明，介绍 OWASP CRS 的概念与本页各功能的用法。</p><h2 id="字段说明" tabindex="-1"><a class="header-anchor" href="#字段说明"><span>字段说明</span></a></h2><h3 id="调参核心概念" tabindex="-1"><a class="header-anchor" href="#调参核心概念"><span>调参核心概念</span></a></h3><table><thead><tr><th>概念</th><th>说明</th></tr></thead><tbody><tr><td>引擎模式</td><td>On=正常拦截；DetectionOnly=只记录不拦截（灰度观察）；Off=完全关闭 Coraza。</td></tr><tr><td>Paranoia Level（PL，偏执级别）</td><td>规则严格度档位，1~4。PL 越高规则越多、检测越严，但误报也越多。SamWaf 拆为「拦截 PL」与「检测 PL」。</td></tr><tr><td>拦截 PL</td><td>实际触发拦截的档位，默认 1。</td></tr><tr><td>检测 PL</td><td>可观察命中但不拦截的档位，用于灰度评估，必须 ≥ 拦截 PL，默认 2。</td></tr><tr><td>Anomaly Score（累计风险分）</td><td>CRS 按规则严重级别累加分值，总分超过阈值才真正拦截（CRITICAL=+5、ERROR=+4、WARNING=+3、NOTICE=+2）。</td></tr><tr><td>入站阈值</td><td>请求侧累计分超过该值触发拦截，官方默认 5。</td></tr><tr><td>出站阈值</td><td>响应侧（泄漏 / 报错等）累计分阈值，默认 4。</td></tr><tr><td>提前拦截</td><td>是否在 Phase 1（读请求头时）额外评估一次；Phase 2 标准拦截始终生效。</td></tr></tbody></table><h3 id="规则状态" tabindex="-1"><a class="header-anchor" href="#规则状态"><span>规则状态</span></a></h3><table><thead><tr><th>状态</th><th>说明</th></tr></thead><tbody><tr><td>默认</td><td>使用上游 CRS 的原始规则，未做改动。</td></tr><tr><td>已禁用</td><td>已通过 <code>SecRuleRemoveById</code> 移除（写入 <code>overrides/10-disabled-rules.conf</code>）。</td></tr><tr><td>已改写</td><td>已用自定义内容覆盖（写入 <code>overrides/20-custom-rules.conf</code>，升级不覆盖）。</td></tr></tbody></table><h2 id="常见问题" tabindex="-1"><a class="header-anchor" href="#常见问题"><span>常见问题</span></a></h2><ul><li><strong>改写 / 禁用规则后会被升级覆盖吗？</strong> 不会。用户改动写入 <code>overrides/</code> 下的独立配置文件，在线升级仅更新上游规则包并保留你的改动。</li><li><strong>命中统计为什么是空的？</strong> 需要有请求经过 OWASP 检测后才会产生数据，且统计在重启后清零。</li><li><strong>怎样在不改全局参数的情况下放行某些请求？</strong> 使用「URL 白名单」或「IP 白名单」对特定 URL / IP 跳过 OWASP 检测。</li><li><strong>调参后需要重启吗？</strong> 不需要，调参与规则改动修改后会自动热重载生效。</li></ul>',35)])])}const h=t(s,[["render",n]]),c=JSON.parse('{"path":"/guide/Owasp.html","title":"OWASP 规则集管理","lang":"zh-CN","frontmatter":{},"git":{"createdTime":1782802574000,"updatedTime":1782802574000,"contributors":[{"name":"samwaf","username":"samwaf","email":"samwafgo@gmail.com","commits":1,"url":"https://github.com/samwaf"}]},"readingTime":{"minutes":5.92,"words":1776},"filePathRelative":"guide/Owasp.md"}');export{h as comp,c as data};
