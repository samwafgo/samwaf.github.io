import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as a,a as n,o as s}from"./app-Bdzwg0Tf.js";const o={};function r(i,e){return s(),a("div",null,[...e[0]||(e[0]=[n('<h1 id="owasp-rule-set-management" tabindex="-1"><a class="header-anchor" href="#owasp-rule-set-management"><span>OWASP Rule Set Management</span></a></h1><h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview"><span>Overview</span></a></h2><p>The OWASP Core Rule Set (CRS) online management page manages SamWaf&#39;s built-in OWASP CRS rules (powered by the Coraza engine). It brings rule browsing, tuning, hit statistics, online upgrade, sandbox testing, change log, and usage notes together on one page, switched via the tabs at the top.</p><p>Supported capabilities include: viewing the rule list, enabling / disabling / overriding rules, adjusting thresholds and PL (Paranoia Level), online upgrades (preserving user changes), sandbox testing, hit statistics, and usage notes.</p><p>The page has 7 tabs: <strong>Rules, Tuning, Hit Stats, Online Upgrade, Sandbox, Change Log, Usage</strong>.</p><h2 id="tabs" tabindex="-1"><a class="header-anchor" href="#tabs"><span>Tabs</span></a></h2><h3 id="_1-rules" tabindex="-1"><a class="header-anchor" href="#_1-rules"><span>1. Rules</span></a></h3><p>View and manage each CRS rule.</p><ul><li><strong>Filtering</strong>: filter by source file, severity, PL, and status (Default / Disabled / Modified); search by keyword / rule ID / description; use &quot;My Changes&quot; to see only rules you modified, and &quot;Hot Reload&quot; to apply changes immediately.</li><li><strong>Actions</strong> per rule: <strong>View / Enable / Disable / Override / Reset</strong>. <ul><li><strong>Disable</strong>: adds a <code>SecRuleRemoveById</code> entry to <code>overrides/10-disabled-rules.conf</code> without modifying the original rule files; takes effect after hot reload.</li><li><strong>Override</strong>: edit the rule content using Coraza / ModSecurity syntax; saved to <code>overrides/20-custom-rules.conf</code> and <strong>not overwritten by upstream upgrades</strong>.</li><li><strong>AI Analyze</strong>: optionally send the rule content and metadata to the AI assistant to explain the rule&#39;s protection intent, trigger conditions, and possible false-positive scenarios (confirm the content before sending).</li></ul></li><li><strong>Field meanings</strong>: the detail view explains rule ID, execution phase (Phase 1–5), severity, Paranoia Level, tags, description, directive type, etc.</li></ul><h3 id="_2-tuning" tabindex="-1"><a class="header-anchor" href="#_2-tuning"><span>2. Tuning</span></a></h3><p>Adjust the overall behavior and thresholds of the rule engine. SamWaf&#39;s defaults are already more lenient than the official ones to reduce false positives; changes hot-reload automatically.</p><p>Key parameters:</p><ul><li><strong>Engine mode</strong>: On (blocking) / DetectionOnly (observe only) / Off.</li><li><strong>Blocking Paranoia Level</strong>: range 1-4, higher is stricter, default 1.</li><li><strong>Detection Paranoia Level</strong>: must be ≥ the blocking PL; rules above the blocking PL are only logged, not blocked; default 2.</li><li><strong>Inbound / Outbound Anomaly thresholds</strong>: blocking is triggered only when the cumulative risk score reaches this value. Inbound official default is 5 (SamWaf is more lenient); outbound default is 4.</li><li><strong>Early blocking</strong>: when on, an extra evaluation is done in Phase 1 (after reading only the request headers). Regardless of this switch, the standard Phase 2 blocking (after reading the request body) always applies.</li><li><strong>Request body size limit / in-memory limit / inspection byte limit</strong>: control how much of the request body Coraza buffers and inspects, in bytes; <code>0</code> usually means use the default.</li><li><strong>CRS transaction variables (tx.*)</strong>: written as <code>SecAction setvar</code> to <code>overrides/00-tuning.conf</code> — safer than overriding rules directly and not lost when CRS is upgraded.</li></ul><blockquote><p>If you only want to skip OWASP inspection for specific URLs or IPs, there is no need to change global parameters — use the &quot;URL Whitelist&quot; or &quot;IP Whitelist&quot;.</p></blockquote><h3 id="_3-hit-stats" tabindex="-1"><a class="header-anchor" href="#_3-hit-stats"><span>3. Hit Stats</span></a></h3><p>Counts how many times each rule was hit while running (reset to zero on restart), helping you identify high-frequency false-positive rules and providing a basis for disabling or tuning.</p><ul><li>Sort by total / blocked / detected counts, and view summaries such as total blocked, total detected, and the most-hit rule.</li><li>The list shows severity, rule message, total hits, blocked, detected, last seen, etc.</li><li>Supports &quot;Reset&quot; of statistics (irreversible) and a &quot;View Rule&quot; jump to the Rules tab.</li></ul><h3 id="_4-online-upgrade" tabindex="-1"><a class="header-anchor" href="#_4-online-upgrade"><span>4. Online Upgrade</span></a></h3><p>Pulls the latest rule package from the SamWaf upgrade server. Upgrades <strong>preserve the rule IDs you have overridden</strong> and only update the upstream version.</p><ul><li>Shows current version, latest version, whether an update is needed, last check time, and the changelog.</li><li>Click <strong>Check Update</strong> to compare versions; click <strong>Upgrade Now</strong> to run the upgrade in the background. A push notification is sent when it completes.</li></ul><h3 id="_5-sandbox" tabindex="-1"><a class="header-anchor" href="#_5-sandbox"><span>5. Sandbox</span></a></h3><p>Construct an HTTP request and feed it directly to the current Coraza instance to see which rules are hit and the anomaly score — useful for verifying the effect of tuning or overrides.</p><ul><li>Enter method, URL, headers (one <code>Key: Value</code> per line), and body, or &quot;Load Example&quot;.</li><li>After running, the result shows whether it was blocked, the blocking score, detection score, blocking threshold, and per-rule details (severity, score, whether it counts toward blocking, phase, rule message, rule file).</li><li>You can <strong>Disable / Override</strong> a hit rule directly from the result, and diagnostic hints are provided (e.g. hit but below threshold, not counted toward blocking due to PL filtering).</li></ul><h3 id="_6-change-log" tabindex="-1"><a class="header-anchor" href="#_6-change-log"><span>6. Change Log</span></a></h3><p>Records rule operations initiated through the SamWaf UI or API (manually editing files is not logged here).</p><ul><li>The list includes time, rule ID, action type (Disable / Enable / Override / Reset / Tuning), source file, and note.</li><li>Refresh, and jump from a record to the corresponding rule.</li></ul><h3 id="_7-usage" tabindex="-1"><a class="header-anchor" href="#_7-usage"><span>7. Usage</span></a></h3><p>Built-in usage notes that introduce OWASP CRS concepts and how to use each feature on this page.</p><h2 id="field-reference" tabindex="-1"><a class="header-anchor" href="#field-reference"><span>Field Reference</span></a></h2><h3 id="core-tuning-concepts" tabindex="-1"><a class="header-anchor" href="#core-tuning-concepts"><span>Core Tuning Concepts</span></a></h3><table><thead><tr><th>Concept</th><th>Description</th></tr></thead><tbody><tr><td>Engine mode</td><td>On = normal blocking; DetectionOnly = log only, no blocking (canary observation); Off = Coraza fully disabled.</td></tr><tr><td>Paranoia Level (PL)</td><td>Rule strictness tier, 1–4. Higher PL means more rules and stricter detection, but more false positives. SamWaf splits it into &quot;Blocking PL&quot; and &quot;Detection PL&quot;.</td></tr><tr><td>Blocking PL</td><td>The tier that actually triggers blocking, default 1.</td></tr><tr><td>Detection PL</td><td>The tier that can observe hits without blocking, for canary evaluation; must be ≥ Blocking PL, default 2.</td></tr><tr><td>Anomaly Score</td><td>CRS accumulates a score per rule by severity; blocking happens only when the total exceeds the threshold (CRITICAL=+5, ERROR=+4, WARNING=+3, NOTICE=+2).</td></tr><tr><td>Inbound threshold</td><td>Blocking is triggered when the request-side cumulative score exceeds this value; official default 5.</td></tr><tr><td>Outbound threshold</td><td>Cumulative-score threshold on the response side (leaks / errors, etc.), default 4.</td></tr><tr><td>Early blocking</td><td>Whether to do an extra evaluation in Phase 1 (when reading headers); the standard Phase 2 blocking always applies.</td></tr></tbody></table><h3 id="rule-status" tabindex="-1"><a class="header-anchor" href="#rule-status"><span>Rule Status</span></a></h3><table><thead><tr><th>Status</th><th>Description</th></tr></thead><tbody><tr><td>Default</td><td>Uses the original upstream CRS rule, unchanged.</td></tr><tr><td>Disabled</td><td>Removed via <code>SecRuleRemoveById</code> (written to <code>overrides/10-disabled-rules.conf</code>).</td></tr><tr><td>Modified</td><td>Overridden with custom content (written to <code>overrides/20-custom-rules.conf</code>, not overwritten by upgrades).</td></tr></tbody></table><h2 id="faq" tabindex="-1"><a class="header-anchor" href="#faq"><span>FAQ</span></a></h2><ul><li><strong>Will overriding / disabling rules be overwritten by upgrades?</strong> No. User changes are written to separate config files under <code>overrides/</code>; online upgrades only update the upstream rule package and preserve your changes.</li><li><strong>Why are hit statistics empty?</strong> Data only appears after requests pass through OWASP inspection, and statistics reset to zero on restart.</li><li><strong>How do I allow certain requests without changing global parameters?</strong> Use the &quot;URL Whitelist&quot; or &quot;IP Whitelist&quot; to skip OWASP inspection for specific URLs / IPs.</li><li><strong>Do I need to restart after tuning?</strong> No. Tuning and rule changes hot-reload automatically.</li></ul>',35)])])}const h=t(o,[["render",r]]),u=JSON.parse('{"path":"/en/guide/Owasp.html","title":"OWASP Rule Set Management","lang":"en-US","frontmatter":{},"git":{"createdTime":1782802574000,"updatedTime":1782802574000,"contributors":[{"name":"samwaf","username":"samwaf","email":"samwafgo@gmail.com","commits":1,"url":"https://github.com/samwaf"}]},"readingTime":{"minutes":3.79,"words":1137},"filePathRelative":"en/guide/Owasp.md"}');export{h as comp,u as data};
