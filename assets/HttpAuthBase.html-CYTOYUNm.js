import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as s,a,o}from"./app-SCCtw_Dg.js";const i={};function n(r,e){return o(),s("div",null,[...e[0]||(e[0]=[a('<h1 id="website-access-authentication" tabindex="-1"><a class="header-anchor" href="#website-access-authentication"><span>Website Access Authentication</span></a></h1><h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview"><span>Overview</span></a></h2><p>Website access authentication lets you require a password to access a protected website. After a website password is enabled, visitors must access the site with the password. The authentication accounts (username + password) are managed on this page, while the authentication type and authentication path prefix are configured on the website edit page.</p><h2 id="prerequisites" tabindex="-1"><a class="header-anchor" href="#prerequisites"><span>Prerequisites</span></a></h2><p>Authentication accounts work together with the website&#39;s &quot;Web Password Visit&quot; switch:</p><ol><li>Open the website edit page and turn <strong>Web Password Visit</strong> on (after enabling, the website requires a password for access; please add a username and password).</li><li>Choose an <strong>Authentication Type</strong>: <ul><li><strong>Authorization Mode (Browser Popup)</strong>: standard HTTP Basic Auth, authenticated through the browser popup.</li><li><strong>Custom Mode (Custom Page)</strong>: a custom login page, suitable for avoiding conflicts when the backend site also uses Authorization authentication.</li></ul></li><li>Set the <strong>Auth Path Prefix</strong>: a custom HTTP authentication path prefix to hide system features. Leave it empty for an auto-generated random path. Format: start with <code>/_</code>, at least 10 characters, lowercase letters, numbers, and underscores only. Example: <code>/_waf_a7k3m9x2</code>.</li><li>Optionally set <strong>Session lifetime / Idle timeout / Bind to login IP</strong> - see &quot;Session lifetime settings&quot; below.</li><li>Add access accounts (username + password) on this page.</li></ol><h2 id="steps" tabindex="-1"><a class="header-anchor" href="#steps"><span>Steps</span></a></h2><h3 id="add-an-account" tabindex="-1"><a class="header-anchor" href="#add-an-account"><span>Add an account</span></a></h3><ol><li>Click <strong>New</strong>.</li><li>Enter a <strong>Username</strong> and <strong>Password</strong>.</li><li>Click <strong>Confirm</strong> to save.</li></ol><blockquote><p>Note: the password must not contain a colon <code>:</code>, otherwise a &quot;Password Not Normal&quot; message appears.</p></blockquote><h3 id="search-edit-delete" tabindex="-1"><a class="header-anchor" href="#search-edit-delete"><span>Search / Edit / Delete</span></a></h3><ul><li>Enter a keyword in the <strong>Username</strong> box at the top and click <strong>Search</strong>.</li><li>In the list action column, click <strong>Edit</strong> to modify an account or <strong>Delete</strong> to remove it.</li></ul><h2 id="session-lifetime-settings" tabindex="-1"><a class="header-anchor" href="#session-lifetime-settings"><span>Session lifetime settings</span></a></h2><p>Configured under &quot;Website password access&quot; on the website edit page; they control how long one sign-in lasts.</p><table><thead><tr><th>Setting</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td>Session lifetime</td><td>1440 minutes (24 hours)</td><td>Counted from the moment of sign-in: past this duration the password must be entered again, no matter how actively the site is being used.</td></tr><tr><td>Idle timeout</td><td>0 (off)</td><td>Sign out automatically after this long without any request. 0 disables it, leaving only the session lifetime. <strong>Custom mode only</strong>.</td></tr><tr><td>Bind to login IP</td><td>On</td><td>When on, the credential stops working from a different IP, so a copied cookie is useless elsewhere.</td></tr></tbody></table><div class="hint-container tip"><p class="hint-container-title">Why Authorization mode has no idle timeout</p><p>Under HTTP Basic the browser caches the password and replays it on every request, so the server cannot tell whether the user has actually left - &quot;idle&quot; is not an observable state in that mode. Switch to Custom mode if you need idle timeout.</p></div><div class="hint-container warning"><p class="hint-container-title">Bind to login IP and CDNs</p><p>The client IP used for authentication is resolved through the website&#39;s &quot;Real client IP source&quot; setting, the same one used by access logs. For a website behind a CDN or a front Nginx this binds to the visitor&#39;s real IP: switching mobile towers or reconnecting a home line will drop the login. Turn this off if it happens too often.</p></div><p>Idle timeout can take effect up to one minute later than configured: to avoid a database write per request, the last-active time is refreshed at most once every 60 seconds.</p><h2 id="online-sessions" tabindex="-1"><a class="header-anchor" href="#online-sessions"><span>Online sessions</span></a></h2><p>Once website password access is enabled, the website edit page lists the visitors currently signed in with the password.</p><table><thead><tr><th>Column</th><th>Description</th></tr></thead><tbody><tr><td>Username / Method</td><td>Who signed in, and whether through the browser popup or the custom page.</td></tr><tr><td>Login IP / Location</td><td>The visitor&#39;s real IP and its location (resolved through the website&#39;s &quot;Real client IP source&quot;).</td></tr><tr><td>Login time / Last active / Expires at / Remaining</td><td>The timeline of this sign-in. Less than 10 minutes remaining is highlighted in orange.</td></tr><tr><td>Status</td><td>Online, or revoked (hover to see the reason).</td></tr></tbody></table><p>Three ways to sign someone out: <strong>Kick</strong> (one session), <strong>Kick user</strong> (every session of that user on this site) and <strong>Kick all</strong> (every visitor of this site - use it when a password leak is suspected).</p><div class="hint-container warning"><p class="hint-container-title">How fast a kick takes effect, and its limits</p><ul><li>Because sessions are cached for up to 60 seconds, a kick takes effect within 60 seconds at worst.</li><li><strong>In Authorization mode (browser popup), a kick can only force the person to type the password once more.</strong> HTTP Basic has no logout mechanism; the server can only change the authentication realm to make the browser prompt again, and the same username and password will sign in successfully after that. <strong>To keep someone out for good, delete their account.</strong></li><li>In Custom mode (custom page) a kick is a clean one-way revocation and the visitor must sign in again.</li></ul></div><p>Deleting or modifying an account (renaming or changing its password) revokes that account&#39;s sessions automatically - no manual kick needed.</p><p>Revoked session records are kept for 30 days for later investigation, then cleaned up automatically.</p><h2 id="security-audit" tabindex="-1"><a class="header-anchor" href="#security-audit"><span>Security audit</span></a></h2><p>Sign-in success, wrong password, lockout, kick and session expiry are all recorded under <strong>System settings - Security audit</strong> in the &quot;Website password&quot; category, and can be filtered by category, event, account, source IP and website.</p><p>Denied requests from visitors who are not signed in are recorded as well, but throttled to one entry per IP + website per 5 minutes: a single directory scan means thousands of unauthenticated requests, and without throttling they would flood the audit table and bury the records that matter.</p><h2 id="field-reference" tabindex="-1"><a class="header-anchor" href="#field-reference"><span>Field Reference</span></a></h2><h3 id="authentication-account-this-page" tabindex="-1"><a class="header-anchor" href="#authentication-account-this-page"><span>Authentication account (this page)</span></a></h3><table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td>Username</td><td>The login username used to access the website.</td></tr><tr><td>Password</td><td>The login password used to access the website; it must not contain a colon <code>:</code>.</td></tr></tbody></table><h3 id="related-settings-on-the-website-edit-page" tabindex="-1"><a class="header-anchor" href="#related-settings-on-the-website-edit-page"><span>Related settings on the website edit page</span></a></h3><table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td>Web Password Visit</td><td>After enabling, the website requires a password for access. Please add a username and password.</td></tr><tr><td>Authentication Type</td><td>Authorization mode: standard HTTP Basic Auth (browser popup); Custom mode: custom login page, suitable for avoiding conflicts when the backend also uses Authorization authentication.</td></tr><tr><td>Auth Path Prefix</td><td>Custom HTTP authentication path prefix to hide system features. Leave empty for an auto-generated random path. Format: start with <code>/_</code>, at least 10 characters, lowercase letters, numbers, and underscores only. Example: <code>/_waf_a7k3m9x2</code>.</td></tr><tr><td>Session lifetime</td><td>How long one sign-in lasts at most, in minutes. Default 1440 (24 hours).</td></tr><tr><td>Idle timeout</td><td>Sign out after this long without any request, in minutes. 0 disables it. Custom mode only.</td></tr><tr><td>Bind to login IP</td><td>Whether the credential is bound to the IP used at sign-in. On by default.</td></tr></tbody></table><h2 id="faq" tabindex="-1"><a class="header-anchor" href="#faq"><span>FAQ</span></a></h2><ul><li><strong>&quot;Password Not Normal&quot; message?</strong> The password must not contain a colon <code>:</code>. Remove the colon and try again.</li><li><strong>Account added but the website does not ask for a password?</strong> Make sure <strong>Web Password Visit</strong> is enabled on the website edit page.</li><li><strong>When should I use Custom mode?</strong> When the backend website itself also uses Authorization authentication, use Custom mode (a custom login page) to avoid authentication conflicts.</li><li><strong>I kicked someone but they still seem to have access?</strong> Sessions are cached for up to 60 seconds, so wait a moment. In Authorization mode, note that a kick can only force the password prompt again - anyone who knows the username and password can sign back in. Delete the account to block them for good.</li><li><strong>Visitors are asked to sign in again very often?</strong> Usually &quot;Bind to login IP&quot; combined with a changing visitor address (switching mobile towers, a new PPPoE session). Turn &quot;Bind to login IP&quot; off on the website edit page.</li><li><strong>Does everyone have to sign in again after the upgrade?</strong> Yes - the upgrade invalidates existing credentials once, after which your configured lifetime applies normally.</li></ul>',35)])])}const c=t(i,[["render",n]]),l=JSON.parse('{"path":"/en/guide/HttpAuthBase.html","title":"Website Access Authentication","lang":"en-US","frontmatter":{},"git":{"createdTime":1782790385000,"updatedTime":1788652623000,"contributors":[{"name":"samwaf","username":"samwaf","email":"samwafgo@gmail.com","commits":2,"url":"https://github.com/samwaf"}]},"readingTime":{"minutes":4.2,"words":1260},"filePathRelative":"en/guide/HttpAuthBase.md"}');export{c as comp,l as data};
