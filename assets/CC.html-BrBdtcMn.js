import{_ as t}from"./cc-XEjzRfqF.js";import{_ as o}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as s,a as r,o as n}from"./app-SCCtw_Dg.js";const a={};function i(d,e){return n(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="cc-protection" tabindex="-1"><a class="header-anchor" href="#cc-protection"><span>CC Protection</span></a></h1><h2 id="overview" tabindex="-1"><a class="header-anchor" href="#overview"><span>Overview</span></a></h2><p>CC protection defends against CC (Challenge Collapsar) attacks - large volumes of requests sent to a site in a short time.</p><p>A website can hold <strong>multiple rules</strong>, evaluated top-down by priority and stopping at the first match (Observe rules excepted). Every rule has four parts:</p><table><thead><tr><th>Part</th><th>Question it answers</th></tr></thead><tbody><tr><td>Scope</td><td>Which requests <strong>this rule covers</strong></td></tr><tr><td>Counting</td><td>Which matched requests <strong>count as one hit</strong>, and how hits are <strong>grouped</strong></td></tr><tr><td>Threshold</td><td><strong>How many times</strong> within <strong>how long</strong></td></tr><tr><td>When exceeded</td><td><strong>What to do</strong> once exceeded</td></tr></tbody></table><p>Rules are configured per website; choosing the Global site applies them everywhere. Execution order is <strong>this site&#39;s rules, then the global site&#39;s rules</strong>.</p><figure><img src="'+t+'" alt="CC Protection" tabindex="0" loading="lazy"><figcaption>CC Protection</figcaption></figure><h2 id="steps" tabindex="-1"><a class="header-anchor" href="#steps"><span>Steps</span></a></h2><ol><li>Open the <strong>CC Protection</strong> page.</li><li>Pick a <strong>group</strong> and a <strong>website</strong> at the top (groups are only a filter and never affect protection). <ul><li>Leaving the website on <strong>All websites</strong> (the default) lists every site&#39;s rules with an extra <strong>Website</strong> column, which is handy for reviewing everything at once. <strong>Up / Down</strong> then reorders only within the same site.</li><li>Do not confuse the two: <strong>All websites</strong> is a viewing range and only changes what you see, whereas the <strong>Global site</strong> in the dropdown is a real configuration target - rules placed on it apply to every site.</li></ul></li><li>Click <strong>New rule</strong> and fill in the four sections:</li></ol><h3 id="_1-scope" tabindex="-1"><a class="header-anchor" href="#_1-scope"><span>1. Scope</span></a></h3><ul><li><strong>All requests</strong> - the rule covers every request to this website.</li><li><strong>By conditions</strong> - add conditions one by one, combined with AND. Each condition is &quot;target + operator + value&quot;. When the target is a request header, cookie, query parameter or JSON body field, you also supply a <strong>field name</strong> saying which one to read.</li><li><strong>Advanced (script)</strong> - express complex conditions in script. This and &quot;By conditions&quot; are two ways to write the same thing - <strong>pick one</strong>.</li></ul><blockquote><p>The operator decides what goes on the right: exists / not exists needs no value; in / not in is multi-select; between takes two numbers; everything else takes one value.</p></blockquote><blockquote><p>Request method, scheme, file extension, response status code, response Content-Type and the bot flag have enumerable values, so the right-hand side becomes a dropdown: <strong>pick a common value or type your own</strong>. Note that the file extension includes the dot (<code>.js</code>, not <code>js</code>), and a real response Content-Type usually carries <code>; charset=utf-8</code>, which is why it defaults to prefix match rather than equals.</p></blockquote><h3 id="_2-counting" tabindex="-1"><a class="header-anchor" href="#_2-counting"><span>2. Counting</span></a></h3><ul><li><strong>Count scope</strong><ul><li><code>Exclude static assets</code> (recommended) - js, css and images are not counted. A single page load pulls dozens of sub-requests, and counting them distorts the threshold. This is the most common reason a threshold ends up wrong.</li><li><code>All requests</code> - everything counts.</li><li><code>Document requests only</code> - counts only real page views.</li></ul></li><li><strong>Group by</strong> - decides how hits are bucketed. <ul><li><code>Client IP</code> (default)</li><li><code>IP + URL</code> - the same IP is counted separately per endpoint</li><li><code>Cookie value</code> / <code>Header value</code> / <code>Query value</code> - needs a field name; good for limiting per logged-in user or business uid, and avoids limiting a whole NAT exit as one</li><li><code>Whole site</code> - one counter for the entire site</li></ul></li></ul><blockquote><p>⚠️ Cookies, headers and query parameters <strong>can be forged by clients</strong> - an attacker can change the value on every request to bypass the limit, unless an upstream trusted proxy guarantees the field. Visitors that do not carry the field automatically <strong>fall back to per-IP counting</strong>, so they are never merged into one bucket and limited together.</p></blockquote><h3 id="_3-threshold" tabindex="-1"><a class="header-anchor" href="#_3-threshold"><span>3. Threshold</span></a></h3><ul><li><strong>Time window</strong> (seconds) and <strong>request count</strong> - that is, &quot;at most M times in N seconds&quot;.</li><li><strong>Burst tolerance</strong> - absorbs the burst of concurrent sub-requests from one page load; applies only at the start of the window.</li></ul><h3 id="_4-when-exceeded" tabindex="-1"><a class="header-anchor" href="#_4-when-exceeded"><span>4. When exceeded</span></a></h3><table><thead><tr><th>Action</th><th>Description</th></tr></thead><tbody><tr><td><strong>Human verification</strong> (default)</td><td>Shows a captcha. Far cheaper on false positives than banning an IP. Passing starts a challenge-free period as long as the configured duration, so sub-requests of the same page are not challenged again; exceeding the threshold after it expires <strong>asks for verification again</strong>, making this a repeatable gate rather than a one-time toll</td></tr><tr><td><strong>Observe</strong></td><td>Logs only, does not block, <strong>and continues to the following rules</strong>. Recommended when rolling out a new rule</td></tr><tr><td><strong>Block</strong></td><td>Blocks this request</td></tr><tr><td><strong>Ban</strong></td><td>Blocks the source for the configured duration. Scope can be <strong>this site only</strong> (default) or all sites</td></tr></tbody></table><p>You can also set &quot;skip global CC rules after a match&quot;.</p><ul><li><strong>Exempt verified crawlers</strong> (on by default for new rules): search-engine crawlers that completed full identity verification are not counted by this rule. The Observe action is unaffected - it never blocks anyway, and exempting would only hide the crawler&#39;s real volume from you.</li></ul><blockquote><p>Verification follows the two steps the vendors document: reverse-resolve the visiting IP to a hostname and check its suffix, then resolve that hostname forward and confirm it maps back to the original IP. <strong>Only passing both steps counts as verified.</strong> A crawler that matches the suffix but cannot be confirmed by the forward lookup is still treated exactly as before (it is not classified as spoofed), but it does <strong>not</strong> get the exemption - an exemption should never be more confident than the verification behind it.</p><p>If a search engine is crawling hard enough to slow the site down, turn this off and add a condition <code>Is bot = Yes</code>, which rate-limits the crawler without touching real visitors.</p></blockquote><ol start="4"><li>Click <strong>Confirm</strong>. Rules take effect immediately, no restart needed.</li></ol><blockquote><p>Use <strong>Up / Down</strong> in the list to reorder priority, and the switch to disable a rule temporarily. <strong>Show CC Ban IP</strong> lists currently banned sources with their scope and remaining time, and lets you release them.</p></blockquote><h2 id="the-log-says-human-verification-fired-but-no-challenge-appeared" tabindex="-1"><a class="header-anchor" href="#the-log-says-human-verification-fired-but-no-challenge-appeared"><span>The log says human verification fired, but no challenge appeared</span></a></h2><p><strong>Check one thing first</strong>: website settings -&gt; <strong>Human verification</strong> -&gt; <strong>Never challenge these paths</strong>. Paths listed there apply to <strong>every trigger source</strong>, so verification requested by a CC rule is skipped too. The <strong>Rule</strong> column then carries <code>命中验证码排除URL，本次未挑战</code>.</p><blockquote><p>This is by design: a path usually ends up on that list because a challenge would break it (payment callbacks, webhooks, health checks, pure APIs), which is a hard constraint a single rule should not override. To have it challenged, remove it from the list; if you never meant to challenge it, exclude it in the CC rule&#39;s own scope instead - that is clearer.</p></blockquote><p>Once that is ruled out, check the wording in the <strong>Rule</strong> column:</p><table><thead><tr><th>Log text</th><th>Meaning</th></tr></thead><tbody><tr><td><code>CC人机验证:&lt;rule&gt;[&lt;code&gt;]</code></td><td>The visitor was asked to verify; the challenge page was served</td></tr><tr><td><code>CC人机验证-免挑战期内放行:&lt;rule&gt;[&lt;code&gt;]</code></td><td>The visitor passed verification recently and is still inside the challenge-free period set by Duration, so this request went straight through</td></tr></tbody></table><p>The second one is normal, not a fault. Passing verification does not reset the counting window, so without a grace period the very next request would still be over the threshold and would be challenged again, looping forever. Once the grace period ends, exceeding the threshold asks for verification again.</p><p>To shorten that pass-through window, lower the rule&#39;s <strong>Duration</strong> - for the human verification action that value <em>is</em> the challenge-free period.</p><h2 id="a-visitor-was-blocked-which-rule-did-it" tabindex="-1"><a class="header-anchor" href="#a-visitor-was-blocked-which-rule-did-it"><span>A visitor was blocked - which rule did it?</span></a></h2><ul><li><strong>The challenge page shows a Reference ID</strong> at the bottom: the access reference of that one request. Ask the visitor for it, then filter the attack log by <strong>Access reference</strong> to see the rule, time, source IP and URL.</li><li><strong>Each rule in the list carries a short code</strong> below its name (like <code>CC-7F3A2B</code>). The attack log&#39;s <strong>Rule</strong> column includes it, so the code tells you which rule fired.</li></ul><blockquote><p>The short code appears <strong>only in the admin console</strong> and is never shown to visitors. Visitors always get the per-request reference instead - a stable identifier on a public page would let someone probe repeatedly to work out exactly which rule they hit or evaded.</p></blockquote><h2 id="is-this-rule-actually-catching-anyone-the-trigger-board" tabindex="-1"><a class="header-anchor" href="#is-this-rule-actually-catching-anyone-the-trigger-board"><span>Is this rule actually catching anyone? The trigger board</span></a></h2><p>The rule list has a <strong>Triggers</strong> column. Click the number to open that rule&#39;s board:</p><ul><li><strong>Total triggers</strong>, broken down by action (observe / verification / block / ban)</li><li><strong>First</strong> and <strong>last</strong> trigger times</li><li><strong>Top sources</strong>, grouped by the rule&#39;s statistic dimension. Counting by IP shows IPs; counting by <code>IP+URI</code> shows &quot;IP|path&quot;; counting by header or cookie shows that field&#39;s value</li></ul><p>It answers two questions: <strong>is the threshold too low</strong> (lots of ordinary visitors on the list), and <strong>is someone actually attacking</strong> (a few sources far above the rest).</p><p>Three things to know, or the numbers are easy to misread:</p><ol><li><strong>Only triggers are counted, not matches.</strong> A request that falls within a rule&#39;s scope is merely counted toward that rule; it is a trigger only once the threshold is exceeded and the action runs. So a rule showing 0 triggers is not idle - usually the opposite: nobody crossed the line.</li><li><strong>Counted in-process; resets when the WAF restarts.</strong> The board shows the start of the counting window at the top - check that timestamp before reading the numbers. Anything you need kept long term is in the attack log, which is the actual ledger.</li><li><strong>The source list has a cap.</strong> When an attacker rotates sources, only the earliest batch is recorded and the board says so. The total trigger count is unaffected and remains accurate.</li></ol><h2 id="evaluation-order" tabindex="-1"><a class="header-anchor" href="#evaluation-order"><span>Evaluation order</span></a></h2><p>A request goes through:</p><ol><li>Website <strong>protection disabled</strong> - nothing is inspected</li><li>Matches an <strong>IP / URL allowlist</strong> - skips all inspection including CC</li><li>A custom rule says <strong>allow and skip CC</strong> - skips CC entirely</li><li>A CC rule&#39;s <strong>scope does not match</strong> - the request is not counted, move to the next rule</li><li>Matches but is <strong>below the threshold</strong> - no action, move to the next rule</li><li><strong>Above the threshold</strong> - run the &quot;When exceeded&quot; action</li><li>The website has <strong>log-only mode</strong> on - blocking actions from the previous step are downgraded to logging</li></ol><blockquote><p>The same table is behind the ⓘ next to &quot;When exceeded&quot; in the rule form, so you do not have to come back to the manual while configuring.</p></blockquote><p>Two common questions:</p><ul><li><strong>To exempt some requests from CC entirely</strong>: either use a custom rule&#39;s &quot;allow and skip CC&quot; (skips all of CC), or leave them outside the rule&#39;s scope (affects only that rule). The latter is recommended - it is visible right where you configure it.</li><li><strong>Log-only mode vs the Observe action</strong>: the former is a site-wide switch that downgrades blocking for every rule; the latter is one rule&#39;s action. When both apply, the site-wide setting wins.</li></ul><h2 id="emergency-mode-drop-the-gate-while-you-are-under-attack" tabindex="-1"><a class="header-anchor" href="#emergency-mode-drop-the-gate-while-you-are-under-attack"><span>Emergency mode: drop the gate while you are under attack</span></a></h2><p>The CC page has an <strong>Emergency mode</strong> button, equivalent to Cloudflare&#39;s Under Attack Mode.</p><p>Once enabled, <strong>every visitor</strong> to that website must complete a human verification first and is then not challenged again for 30 minutes. It <strong>ignores thresholds</strong> and <strong>does not modify any of your existing CC rules</strong> - turning it off restores the previous behaviour exactly, so you can hit it while under attack and switch it off afterwards without worrying about your configuration.</p><p>Enabling it on the <strong>global website</strong> applies it to every site, which is what you want when many sites are hit at once.</p><h3 id="check-this-one-thing-before-you-press-it" tabindex="-1"><a class="header-anchor" href="#check-this-one-thing-before-you-press-it"><span>Check this one thing before you press it</span></a></h3><p>Emergency mode <strong>shares the same gate</strong> as the Human verification action of CC rules, so the website&#39;s &quot;Never challenge these paths&quot; applies to it too.</p><p><strong>App and API clients cannot run a JS challenge.</strong> If your site has a mobile app or partners calling your API, add those endpoint paths under <strong>Website editor - Human verification - Never challenge these paths</strong> first, or enabling emergency mode will lock all of them out. Configure this <strong>ahead of time</strong> so you do not have to think about it in the middle of an incident.</p><h3 id="auto-turn-off" tabindex="-1"><a class="header-anchor" href="#auto-turn-off"><span>Auto turn-off</span></a></h3><p>You can pick 30 minutes / 1 hour / 6 hours / 24 hours, or &quot;until turned off manually&quot;.</p><p><strong>Pick an expiry.</strong> Emergency mode puts every real visitor through an extra challenge, and forgetting about it costs them. If you choose &quot;until turned off manually&quot;, the CC page keeps a red banner at the top listing the websites still in emergency mode - that banner is the reminder.</p><h3 id="two-boundaries" tabindex="-1"><a class="header-anchor" href="#two-boundaries"><span>Two boundaries</span></a></h3><ul><li>Emergency mode has no effect when the website&#39;s <strong>protection switch is off</strong> - it runs as part of the CC check, which is what rule 1 of the evaluation order covers.</li><li>A custom rule&#39;s &quot;allow and skip CC&quot; skips emergency mode too (rule 3 of the evaluation order).</li></ul><h2 id="what-threshold-should-i-use-let-it-read-your-logs" tabindex="-1"><a class="header-anchor" href="#what-threshold-should-i-use-let-it-read-your-logs"><span>What threshold should I use? Let it read your logs</span></a></h2><p>Next to the <strong>Threshold</strong> field in the rule editor there is a <strong>Recommend from traffic</strong> button. It takes this website&#39;s own access logs, samples them with the counting period / scope / dimension you already filled in, and derives a suggestion from the distribution. <strong>Everything is computed locally; no data leaves the machine.</strong></p><p>The panel shows three things:</p><table><thead><tr><th></th><th>Description</th></tr></thead><tbody><tr><td><strong>Distribution</strong></td><td>P50 / P95 / P99 / max. Read it as &quot;99% of visitors make no more than 42 requests per 60s&quot;</td></tr><tr><td><strong>Chart</strong></td><td>The busiest clients sorted by peak; the red dashed line is the threshold and can be <strong>dragged</strong>, showing live how many clients and requests it would hit</td></tr><tr><td><strong>Three tiers</strong></td><td>Loose P99x5 / Balanced P99x3 (default) / Strict P99x1.5</td></tr></tbody></table><h3 id="applying-a-recommendation-switches-the-action-to-observe" tabindex="-1"><a class="header-anchor" href="#applying-a-recommendation-switches-the-action-to-observe"><span>Applying a recommendation switches the action to Observe</span></a></h3><p>This is deliberate and cannot be turned off. A recommendation is still only history; going straight to a ban means testing on real visitors. Run it for 24 hours, check the attack log to see who got hit, and switch to human verification or a ban once you are satisfied.</p><h3 id="when-no-recommendation-is-given" tabindex="-1"><a class="header-anchor" href="#when-no-recommendation-is-given"><span>When no recommendation is given</span></a></h3><p>Better nothing than a number that looks precise and is wrong - you would configure a ban with it, and being wrong means real visitors are locked out.</p><table><thead><tr><th>Situation</th><th>What the panel says</th></tr></thead><tbody><tr><td>New website / too few samples</td><td>No value; an industry starting point is given instead, with its source</td></tr><tr><td>Dimension is Cookie / header / query parameter</td><td>No value: logs have no reliable way to reconstruct those dimensions. Use the IP dimension for a rough scale</td></tr><tr><td>Scope is &quot;documents only&quot;</td><td>No value: that scope depends on the <code>Sec-Fetch-Dest</code> request header, which cannot be reconstructed reliably. Switch to &quot;exclude static assets&quot;</td></tr><tr><td>The rule also has UA / region conditions</td><td>A value is given, flagged with &quot;the sample was not narrowed by these conditions, real hits will be fewer&quot;</td></tr><tr><td>Very large log volume</td><td>A value is given, flagged as computed over a shortened range</td></tr></tbody></table><blockquote><p>A recommendation only fills in the field. Whether to save it is up to you.</p></blockquote><h2 id="which-ip-does-cc-count-by" tabindex="-1"><a class="header-anchor" href="#which-ip-does-cc-count-by"><span>Which IP does CC count by</span></a></h2><p>Two website-level settings, under <strong>Website settings -&gt; Other settings</strong>:</p><table><thead><tr><th>Setting</th><th>What it does</th></tr></thead><tbody><tr><td><strong>Real IP source</strong></td><td>How the visitor&#39;s real IP is parsed out of <code>X-Forwarded-For</code> and similar headers</td></tr><tr><td><strong>IP extraction mode</strong></td><td>Which one CC uses: <strong>NIC mode</strong> = the connecting IP; <strong>Proxy mode</strong> = the real IP parsed above</td></tr></tbody></table><p>CC counting, ban matching and human-verification marking all use the same value, taken from the settings of the <strong>website being visited</strong>. CC rules attached to the global website follow the visited website too - the IP extraction mode describes what a website is deployed behind, which is a deployment fact; the global website carries no traffic, so its own value means nothing.</p><blockquote><p><strong>A website behind a CDN or Nginx must use Proxy mode.</strong> Otherwise CC sees the origin-facing node&#39;s IP, every visitor is counted as one of a handful of IPs, and a single ban can take out the whole origin-facing node.</p></blockquote><h2 id="field-reference" tabindex="-1"><a class="header-anchor" href="#field-reference"><span>Field reference</span></a></h2><table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td>Website</td><td>The site the rule belongs to; the Global site applies everywhere</td></tr><tr><td>Rule name</td><td>For identification; appears in attack logs</td></tr><tr><td>Rule code</td><td>Assigned automatically and not editable, like <code>CC-7F3A2B</code>; included in the attack log&#39;s Rule column so you can tell which rule fired</td></tr><tr><td>Priority</td><td>Lower number runs first</td></tr><tr><td>Match mode</td><td>All requests / By conditions / Advanced (script)</td></tr><tr><td>Count scope</td><td>Which requests count as one hit</td></tr><tr><td>Group by</td><td>How hits are bucketed</td></tr><tr><td>Time window</td><td>Length of the counting window, in seconds</td></tr><tr><td>Request count</td><td>Maximum hits allowed in the window</td></tr><tr><td>Burst tolerance</td><td>Extra instantaneous burst allowed</td></tr><tr><td>When exceeded</td><td>Observe / Human verification / Block / Ban</td></tr><tr><td>Duration</td><td>How long the action stays in effect, in seconds. Human verification: the challenge-free period after passing; Ban: how long the ban lasts</td></tr><tr><td>Ban scope</td><td>This site only / All sites</td></tr><tr><td>Exempt verified crawlers</td><td>Fully verified search-engine crawlers are not counted by this rule; the Observe action is unaffected</td></tr><tr><td>Enabled</td><td>A disabled rule takes no part in evaluation</td></tr></tbody></table><h2 id="payment-callbacks-and-third-party-webhooks-getting-blocked" tabindex="-1"><a class="header-anchor" href="#payment-callbacks-and-third-party-webhooks-getting-blocked"><span>Payment callbacks and third-party webhooks getting blocked</span></a></h2><p>This traffic is <strong>not a crawler</strong> - its User-Agent is something like <code>okhttp</code>, <code>Java/1.8</code> or <code>Go-http-client</code>, with no crawler signature at all, so &quot;Exempt verified crawlers&quot; does nothing for it. You have to allow it explicitly.</p><p>Three ways, from the narrowest blast radius to the widest:</p><table><thead><tr><th>Approach</th><th>Scope</th><th>When to use</th></tr></thead><tbody><tr><td><strong>Exclude the path in the rule&#39;s scope</strong> (recommended)</td><td>That one rule</td><td>Most cases. It is visible right where you configure it</td></tr><tr><td><strong>A custom rule with &quot;allow and skip CC&quot;</strong></td><td>The whole CC check</td><td>When one source must bypass several CC rules</td></tr><tr><td><strong>IP allowlist</strong></td><td>Every check, including injection and XSS</td><td>Only when the provider&#39;s ranges are stable and fully trusted</td></tr></tbody></table><p>Prefer the first: add a condition such as &quot;URL path not in <code>/pay/notify</code>, <code>/sms/callback</code>&quot; to your site-wide fallback rule, and everything else stays protected.</p><blockquote><p>Treat the allowlist as inventory: record what each entry is for, the URL where the provider publishes those ranges, and when it is next due for review. Providers change ranges routinely, and a list nobody revisits will eventually both block real callbacks and keep allowing addresses that are no longer theirs.</p></blockquote><h2 id="upgrading-from-an-older-version" tabindex="-1"><a class="header-anchor" href="#upgrading-from-an-older-version"><span>Upgrading from an older version</span></a></h2><p>Older versions allowed one CC configuration per website. After upgrading it is <strong>converted into an equivalent rule</strong> named &quot;Default CC protection&quot;, tagged as legacy:</p><ul><li>Count scope stays <code>All requests</code> (not exclude-static)</li><li>Ban scope stays <code>All sites</code></li><li>The original rate-limiting algorithm is kept</li></ul><p><strong>The upgrade does not change your existing protection behavior.</strong> The better new defaults (exclude static assets, human verification, this site only) apply only to rules created afterwards.</p><p>It is worth reviewing the converted rule and switching the count scope to &quot;Exclude static assets&quot; and the ban scope to &quot;This site only&quot; - the threshold then reflects real activity, and one website triggering no longer affects the others on the same instance.</p><blockquote><p>If you used the average rate mode: this version fixes a defect where saving the form produced a different effective threshold than the one applied at startup, reverting again after a restart. Saving and restarting are now consistent, so the effective strictness may differ from before - that is the threshold being restored to what you configured.</p></blockquote><h2 id="default-rules-for-new-websites" tabindex="-1"><a class="header-anchor" href="#default-rules-for-new-websites"><span>Default rules for new websites</span></a></h2><p>Creating a website generates two ready-to-use rules:</p><table><thead><tr><th>Rule</th><th>Scope</th><th>Threshold</th><th>Action</th></tr></thead><tbody><tr><td>Dynamic endpoint rate</td><td>All requests (static excluded)</td><td>600 / 60s</td><td>Human verification, 5 min</td></tr><tr><td>Site-wide fallback</td><td>All requests</td><td>3000 / 60s</td><td>Ban this site, 10 min</td></tr></tbody></table><p>Adjust or delete them to fit your traffic. A website can hold up to 20 rules.</p>',92)])])}const u=o(a,[["render",i]]),g=JSON.parse('{"path":"/en/guide/CC.html","title":"CC Protection","lang":"en-US","frontmatter":{},"git":{"createdTime":1706232245000,"updatedTime":1788405984000,"contributors":[{"name":"samwaf","username":"samwaf","email":"samwafgo@gmail.com","commits":4,"url":"https://github.com/samwaf"}]},"readingTime":{"minutes":10.76,"words":3229},"filePathRelative":"en/guide/CC.md"}');export{u as comp,g as data};
