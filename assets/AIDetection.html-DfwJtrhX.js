import{_ as t}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as s,a,o as i}from"./app-CKXDlIur.js";const o={};function n(l,e){return i(),s("div",null,[...e[0]||(e[0]=[a(`<h1 id="ai-intelligent-detection" tabindex="-1"><a class="header-anchor" href="#ai-intelligent-detection"><span>AI Intelligent Detection</span></a></h1><p>AI Intelligent Detection uses a lightweight machine learning model to identify anomalous requests. It acts as a <strong>supplementary layer</strong> to the existing rule engine (SQL injection, XSS, OWASP, etc.), specifically filling blind spots that rules miss: obfuscated bypasses, encoding tricks, and unknown payloads.</p><blockquote><p>The model is <strong>trained locally using your own logs</strong> — training data never leaves your machine, consistent with SamWaf&#39;s private-deployment principle. Inference runs inside SamWaf in pure Go with no Python, GPU, or external service dependency.</p></blockquote><h2 id="overall-workflow" tabindex="-1"><a class="header-anchor" href="#overall-workflow"><span>Overall Workflow</span></a></h2><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span>Accumulate logs → Export sanitized training data → Train &amp; package locally (.swai) → Upload model → Observe mode to verify false positives → Switch to block mode</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="step-1-—-set-up-the-training-tool-samwafai" tabindex="-1"><a class="header-anchor" href="#step-1-—-set-up-the-training-tool-samwafai"><span>Step 1 — Set Up the Training Tool (SamWafAI)</span></a></h2><p>Training is done in the standalone <code>SamWafAI</code> toolchain. You only need <a href="https://docs.astral.sh/uv/" target="_blank" rel="noopener noreferrer">uv</a> installed:</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">cd</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> SamWafAI</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">uv</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> sync</span><span style="--shiki-light:#A0A1A7;--shiki-light-font-style:italic;--shiki-dark:#7F848E;--shiki-dark-font-style:italic;">          # Creates an isolated environment in one shot</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="step-2-—-export-training-data" tabindex="-1"><a class="header-anchor" href="#step-2-—-export-training-data"><span>Step 2 — Export Training Data</span></a></h2><blockquote><p>⚠️ <strong>Important: Fix rule false positives before exporting</strong> The export uses rule hits as weak training labels by default, but rules can produce false positives — legitimate requests incorrectly blocked — which get mislabeled as &quot;attacks&quot; in the export and bake those errors into the model. To address this:</p><ul><li>The export uses <strong>conservative trust</strong>: without manual confirmation, only high-precision detectors (SQLi/XSS via libinjection, OWASP, RCE, path traversal) are trusted as attack samples. Sources prone to false positives — scanners (UA-based, which flags curl/python-requests and other legitimate tools) and custom rules — <strong>are not counted as attacks without explicit confirmation</strong> (they won&#39;t enter the positive training set).</li><li>In <strong>Defense Logs</strong>, you can <strong>annotate</strong> individual records: click the &quot;AI Label&quot; button in the actions column → choose &quot;False Positive (Normal)&quot;, &quot;Confirmed Attack&quot;, or &quot;Ignore (exclude from training)&quot;. Manual labels <strong>take priority</strong> over rule-based weak labels at export time.</li><li>For <strong>bulk annotation</strong>, go to <strong>AI Model Management → Training Annotations</strong>: a centralized annotation workbench where you can filter by site / time / label status (Unlabeled…) / score, select multiple records, and batch-label them as Normal / Confirmed Attack / Ignore. Click a row&#39;s &quot;Detail&quot; to view the full request (Query/Body/UA) and annotate individually (press 1/2/3 on keyboard for quick labeling when detail is open). Recommended: set &quot;Label Status&quot; to &quot;Unlabeled&quot; and clear batches progressively.</li><li>Suggested workflow: use the annotation workbench to batch-mark obvious false positives as &quot;Normal&quot; and confirmed attacks (especially scanners, which aren&#39;t auto-trusted by default) as &quot;Confirmed Attack&quot;, then export.</li></ul></blockquote><p>Two options — choose either:</p><ol><li><strong>Management UI export (recommended)</strong>: Go to <strong>AI Model Management → Export Training Data</strong>, select the number of days and record limit, and click Export. The system writes sanitized training data to <code>data/ai_export/train_*.jsonl</code> on the server. Weak labels are applied automatically based on rule hits, with a second sanitization pass on sensitive parameters.</li><li><strong>Read the log database directly</strong>: Use SamWafAI to read SamWaf&#39;s SQLite log database:<div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">uv</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> run</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwafai</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> export-data</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --sqlite</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwaf_log.db</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --out</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/train.jsonl</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li></ol><p>If you don&#39;t have enough logs yet, you can train a baseline model using public datasets (see <code>examples/</code> in SamWafAI).</p><h3 id="what-s-inside-an-export-file-important" tabindex="-1"><a class="header-anchor" href="#what-s-inside-an-export-file-important"><span>What&#39;s Inside an Export File (Important)</span></a></h3><p>Each export produces <strong>one</strong> <code>train_*.jsonl</code> file containing <strong>two merged parts</strong> — it&#39;s not just your manually labeled records:</p><table><thead><tr><th>Part</th><th>Source</th><th style="text-align:center;">Subject to export limits (days/count)?</th><th>How labels are assigned</th></tr></thead><tbody><tr><td>Part 1</td><td>Logs you have <strong>not</strong> annotated</td><td style="text-align:center;">✅ Yes</td><td>Rule weak labels + <strong>conservative trust</strong>: rule-passed → normal sample; rule-flagged → only high-confidence detectors (SQLi/XSS/OWASP/RCE/traversal) are accepted; low-confidence (scanner/custom) without confirmation are discarded</td></tr><tr><td>Part 2</td><td><strong>All</strong> your manual annotations</td><td style="text-align:center;">❌ No</td><td>Uses the <strong>request snapshot captured at annotation time</strong>: Confirmed Attack → positive sample (with your chosen category), False Positive → negative sample, Ignore → discarded. Labeled with <code>source=manual</code></td></tr></tbody></table><p>Key points:</p><ul><li><strong>It&#39;s not only your annotations</strong> — the file merges &quot;automatic weak labels (records you haven&#39;t touched) + all your manual annotations&quot;.</li><li><strong>Manual labels win on conflict</strong>: any record you&#39;ve annotated is skipped in Part 1 and only processed via Part 2, so your corrections are never overwritten by rule weak labels.</li><li>Part 2 uses the <strong>snapshot frozen at annotation time</strong>, so it is <strong>not subject to the export window (days/count)</strong>. Even if the original log has been purged or falls outside the export window, your annotations still produce samples — nothing is lost.</li></ul><h2 id="step-3-—-train-and-package" tabindex="-1"><a class="header-anchor" href="#step-3-—-train-and-package"><span>Step 3 — Train and Package</span></a></h2><blockquote><p>💡 <strong>Don&#39;t want to install Python?</strong> Download the Python-free trainer from <a href="https://github.com/samwafgo/SamWafAI/releases" target="_blank" rel="noopener noreferrer">SamWafAI Releases</a> (Windows64 / Linux64 single binary). Replace <code>uv run samwafai</code> in the commands below with <code>./samwafai</code> (or <code>samwafai.exe</code>) — usage is identical and data stays on your machine.</p></blockquote><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">uv</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> run</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwafai</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> train</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">   --train</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/train.jsonl</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --out</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/model_lgbm.txt</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">uv</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> run</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwafai</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> eval</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">    --model</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/model_lgbm.txt</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --test</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/train.jsonl</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --json-out</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/eval.json</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">uv</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> run</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwafai</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> package</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --model</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/model_lgbm.txt</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --version</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> myorg-v1</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --eval-json</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/eval.json</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --out</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/model.swai</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>eval</code> prints recall and recommended thresholds at different <strong>false-positive rates</strong> — WAF deployments care most about false positives, so set the block threshold at the ≤ 0.1% false-positive mark (<code>package</code> adopts <code>eval</code>&#39;s recommended threshold automatically).</p><p>You can also run the full pipeline in one command:</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">uv</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> run</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> samwafai</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> pipeline</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --train</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/train.jsonl</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --test</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/test.jsonl</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --version</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> myorg-v1</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;"> --out</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> build/model.swai</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><h2 id="step-4-—-upload-the-model-and-enable" tabindex="-1"><a class="header-anchor" href="#step-4-—-upload-the-model-and-enable"><span>Step 4 — Upload the Model and Enable</span></a></h2><ol><li>Go to <strong>AI Model Management</strong>, upload <code>model.swai</code>. The upload verifies the feature version and file integrity; once passed, the model <strong>hot-reloads immediately — no restart required</strong>.</li><li>In <strong>System Settings</strong>, set <code>ai_enable</code> to <code>1</code> and <code>ai_mode</code> to <code>observe</code>.</li><li>In the <strong>Site Settings → Protection Config</strong> for each site you want to protect, enable &quot;AI Intelligent Detection&quot;.</li></ol><h2 id="step-5-—-observe-→-block" tabindex="-1"><a class="header-anchor" href="#step-5-—-observe-→-block"><span>Step 5 — Observe → Block</span></a></h2><ul><li><strong>Observe mode</strong>: hits are annotated in Defense Logs with a category label (<code>AI Detection:&lt;category&gt;</code>, e.g. <code>AI Detection:SQL Injection</code>) and the score is recorded in a dedicated <strong>&quot;AI Score&quot; column</strong>; requests pass through normally. Run this for <strong>1–2 weeks</strong>. <ul><li>To review: filter Defense Logs by &quot;Log Only = Yes&quot; to isolate AI observation hits, then sort by &quot;AI Score&quot; descending and review from the top.</li></ul></li><li>Verify whether AI hits are real attacks or false positives. False-positive samples can be annotated, re-exported, and used to train a new model version.</li><li>Once the false-positive rate is acceptable, switch <code>ai_mode</code> to <code>block</code> — only requests reaching the block threshold will be blocked.</li></ul><h2 id="step-6-—-threshold-reference-block-observe" tabindex="-1"><a class="header-anchor" href="#step-6-—-threshold-reference-block-observe"><span>Step 6 — Threshold Reference (Block / Observe)</span></a></h2><p>The model management page shows two thresholds for the loaded model, e.g. &quot;Block 0.974 / Observe 0.6818&quot;. These come from <code>manifest.json</code> inside the <code>.swai</code> package, computed by <code>samwafai eval</code> on the test set and written by <code>samwafai package</code>. The engine uses them to determine hits.</p><ul><li><strong>Block threshold</strong>: corresponds to the score at a false-positive rate ≤ 0.1%. Calculated by sorting all <strong>normal requests</strong> in the test set by model score and taking the 99.9th percentile. Meaning: at this threshold, only ~0.1% of normal requests would be misclassified as attacks. WAF deployments prioritize not blocking legitimate traffic, so the default block line is set at this acceptable false-positive rate.</li><li><strong>Observe threshold</strong>: defaults to block threshold × 0.7 — lower than the block line, allowing more &quot;suspicious but below block&quot; requests into the observation log for easier pre-production review.</li></ul><p>The engine evaluates each request by score:</p><table><thead><tr><th>Condition</th><th>Behavior</th></tr></thead><tbody><tr><td><code>score &lt; observe threshold</code></td><td>Treated as normal, not logged</td></tr><tr><td><code>observe threshold ≤ score &lt; block threshold</code></td><td>Observation hit (logged + AI score, not blocked)</td></tr><tr><td><code>score ≥ block threshold</code> and <code>ai_mode=block</code></td><td>Blocked (in observe mode, only logged)</td></tr></tbody></table><p>To adjust thresholds:</p><ul><li>Specify manually at package time: <code>uv run samwafai package --block-threshold 0.9 --observe-threshold 0.6 ...</code> — omit to use <code>eval</code>&#39;s recommended values.</li><li>To make detection globally stricter/looser: edit <code>RECOMMEND_FP_RATE</code> in <code>samwafai/train/evaluate.py</code> (default <code>0.001</code> = 0.1% false-positive rate; lower = stricter) and the <code>× 0.7</code> observe-threshold coefficient, then re-run <code>eval</code> + <code>package</code>.</li></ul><blockquote><p>Note: The example <code>demo</code> model is trained on synthetic data, so its thresholds will be high (synthetic data is too easy to separate). After training on real logs from your own site, these values will be more realistic and typically more spread out.</p></blockquote><h2 id="fail-safe" tabindex="-1"><a class="header-anchor" href="#fail-safe"><span>Fail-Safe</span></a></h2><p>If the model is missing, fails to load, or throws an inference error, AI detection <strong>automatically skips and passes the request</strong> — normal traffic forwarding is never affected. AI only adds protection, never removes it — enabling it does not weaken your existing rule-based defenses.</p><h2 id="faq" tabindex="-1"><a class="header-anchor" href="#faq"><span>FAQ</span></a></h2><ul><li><strong>Upload error &quot;Feature version mismatch&quot;</strong>: The SamWafAI version used to train the model doesn&#39;t match the current SamWaf engine&#39;s feature version. Retrain and repackage using the matching SamWafAI version.</li><li><strong>No hits in observe mode</strong>: Confirm that global <code>ai_enable=1</code>, &quot;AI Intelligent Detection&quot; is enabled on the site, and the model is loaded (status shows &quot;Ready&quot; in the AI Model Management page).</li><li><strong>Worried about blocking legitimate traffic</strong>: Always run observe mode for a period first, confirm the false-positive rate is acceptable, then switch to block.</li></ul>`,40)])])}const h=t(o,[["render",n]]),c=JSON.parse('{"path":"/en/guide/AIDetection.html","title":"AI Intelligent Detection","lang":"en-US","frontmatter":{},"git":{"createdTime":1782198962000,"updatedTime":1782198962000,"contributors":[{"name":"samwaf","username":"samwaf","email":"samwafgo@gmail.com","commits":1,"url":"https://github.com/samwaf"}]},"readingTime":{"minutes":4.86,"words":1459},"filePathRelative":"en/guide/AIDetection.md"}');export{h as comp,c as data};
